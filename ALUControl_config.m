
function ALUControl_config(this_block)

  % Revision History:
  %
  %   01-Mar-2019  (23:36 hours):
  %     Original code was machine generated by Xilinx's System Generator after parsing
  %     C:\432\ecte432-project-1\ALUControl.vhd
  %
  %

  this_block.setTopLevelLanguage('VHDL');

  this_block.setEntityName('ALUControl');

  % System Generator has to assume that your entity  has a combinational feed through; 
  %   if it  doesn't, then comment out the following line:
  this_block.tagAsCombinational;

  this_block.addSimulinkInport('funct');
  this_block.addSimulinkInport('aluOp');

  this_block.addSimulinkOutport('alucntrl');

  alucntrl_port = this_block.port('alucntrl');
  alucntrl_port.setType('UFix_4_0');

  % -----------------------------
  if (this_block.inputTypesKnown)
    % do input type checking, dynamic output type and generic setup in this code block.

    if (this_block.port('funct').width ~= 6);
      this_block.setError('Input data type for port "funct" must have width=6.');
    end

    if (this_block.port('aluOp').width ~= 2);
      this_block.setError('Input data type for port "aluOp" must have width=2.');
    end

  end  % if(inputTypesKnown)
  % -----------------------------

  % System Generator found no apparent clock signals in the HDL, assuming combinational logic.
  % -----------------------------
   if (this_block.inputRatesKnown)
     inputRates = this_block.inputRates; 
     uniqueInputRates = unique(inputRates); 
     outputRate = uniqueInputRates(1);
     for i = 2:length(uniqueInputRates)
       if (uniqueInputRates(i) ~= Inf)
         outputRate = gcd(outputRate,uniqueInputRates(i));
       end
     end  % for(i)
     for i = 1:this_block.numSimulinkOutports 
       this_block.outport(i).setRate(outputRate); 
     end  % for(i)
   end  % if(inputRatesKnown)
  % -----------------------------

    uniqueInputRates = unique(this_block.getInputRates);


  % Add addtional source files as needed.
  %  |-------------
  %  | Add files in the order in which they should be compiled.
  %  | If two files "a.vhd" and "b.vhd" contain the entities
  %  | entity_a and entity_b, and entity_a contains a
  %  | component of type entity_b, the correct sequence of
  %  | addFile() calls would be:
  %  |    this_block.addFile('b.vhd');
  %  |    this_block.addFile('a.vhd');
  %  |-------------

  %    this_block.addFile('');
  %    this_block.addFile('');
  this_block.addFile('ALUControl.vhd');

return;


